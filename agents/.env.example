# ASI:One Aethel AI Configuration
# 
# Get your API key from: https://asi1.ai/dashboard
# Documentation: https://docs.asi1.ai

# Required: Your ASI:One API key
ASI_ONE_API_KEY=your_api_key_here

# Optional: API endpoint (default: https://api.asi1.ai/v1/chat/completions)
# ASI_ONE_API_ENDPOINT=https://api.asi1.ai/v1/chat/completions

# Optional: Model selection (default: asi1-agentic)
# Available models:
#   - asi1-mini: Fast, lightweight responses
#   - asi1-fast: Balanced speed and quality
#   - asi1-extended: Detailed, comprehensive responses
#   - asi1-agentic: Standard orchestration capabilities
#   - asi1-fast-agentic: Fast orchestration
#   - asi1-extended-agentic: Advanced orchestration
ASI_ONE_MODEL=asi1-agentic

# Optional: Temperature for response creativity (0.0 - 1.0, default: 0.7)
# Lower values = more focused, higher values = more creative
ASI_ONE_TEMPERATURE=0.7

# Optional: Maximum tokens per response (default: 2048)
ASI_ONE_MAX_TOKENS=2048

# Optional: Maximum conversation history length (default: 50)
ASI_ONE_MAX_HISTORY=50

# Optional: Minimum interval between API requests in ms (default: 100)
# Helps with rate limiting
ASI_ONE_MIN_REQUEST_INTERVAL=100

# Optional: Enable streaming responses (default: false)
ASI_ONE_STREAM=false

# Optional: Request timeout in milliseconds (default: 30000)
ASI_ONE_TIMEOUT=30000
